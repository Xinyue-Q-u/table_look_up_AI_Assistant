# Receive the user input (query)
#
# Call llm.py to generate a structured query plan
#
# Call validator.py to validate the query plan
#
# Call sql_builder.py to generate parameterized SQL?
#
# Call executor.py to execute the SQL against the database
#
# Call formatters.py to format and return the result to the frontend/user